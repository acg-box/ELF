#!/usr/bin/env python3
"""Run the complete isolated competitor benchmark through its host scheduler."""

import sys

from benchmark_runner.cli import main


if __name__ == "__main__":
    sys.exit(main())
